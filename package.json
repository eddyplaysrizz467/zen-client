{
  "name": "zen-client",
  "version": "1.0.18",
  "description": "Zen Client Minecraft launcher rebuilt in JavaScript and Electron.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:zenmod": "node scripts/build-zen-mod.js",
    "icons": "node scripts/generate-icons.js",
    "dist": "npm run build:zenmod && npm run icons && electron-builder --win --publish never",
    "release": "npm run build:zenmod && npm run icons && electron-builder --win --publish never"
  },
  "keywords": [
    "minecraft",
    "launcher",
    "electron",
    "javascript"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "build": {
    "appId": "com.eddyplaysrizz467.zenclient",
    "productName": "Zen Client",
    "files": [
      "**/*",
      "!dist/**",
      "!out/**",
      "!release/**",
      "!zen-client-mod/**",
      "!bundled-mods/**",
      "!scripts/**",
      "!.github/**",
      "!**/*.map",
      "!**/.git/**",
      "!**/.venv/**",
      "!**/__pycache__/**"
    ],
    "directories": {
      "buildResources": "build",
      "output": "dist"
    },
    "extraResources": [
      {
        "from": "bundled-mods",
        "to": "bundled-mods"
      }
    ],
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "icon": "build/icon.ico"
    },
    "nsis": {
      "artifactName": "Zen-Client-Setup-${version}.${ext}",
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Zen Client"
    },
    "portable": {
      "artifactName": "Zen-Client-${version}.${ext}"
    }
  },
  "dependencies": {
    "minecraft-launcher-core": "^3.18.2",
    "msmc": "^5.0.5",
    "discord-rpc": "^4.0.1",
    "pngjs": "^7.0.0",
    "electron-updater": "^6.6.2"
  },
  "devDependencies": {
    "electron": "^38.2.0",
    "electron-builder": "^24.13.3",
    "png-to-ico": "^2.1.8"
  }
}
